<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
        <title>ClickLogs</title>
    <%= stylesheet_link_tag    'versions', media: 'all' %>
    <%= csrf_meta_tags %>
</head>

<body>
    <%= render partial: "layouts/versions_shared/navigation" %>

    <!-- Page Content -->
    <div class="container"><br>
        <div class="titulo-card card-home">Central de Logs - Área Administrativa</div><br>

            <%= form_tag versions_search_path, method: :get do %>
                <div class="row">
                    <div class="row">
                        <div class="container">
                            <div class="input-group">
                                <%= text_field_tag(:q, params[:q] , { class:"form-control", placeholder: "Pesquisar por..." }) %>
                                <span class="input-group-btn">
                                    <%= submit_tag("Pesquisar", class:"btn btn-default") %>
                                </span>
                            </div>
                        </div>
                    </div>
                </div>
            <% end %> <br>

                <div class="row">
                    <%= yield %>
                </div>
    </div>    <!-- /.container -->
</body>

</html>
